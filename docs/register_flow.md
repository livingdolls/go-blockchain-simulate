Flow Register
Ketika user melakukan register:
User mengisi: name

Sistem membuat:
fake private key
fake public key
fake address (hash public key)

Insert ke table users:

Return ke frontend:
address
public key
private key
